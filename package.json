{
  "name": "lexar-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:front": "npm --prefix apps/lex-front run build",
    "start:front": "npm --prefix apps/lex-front run start",
    "build:admin": "npm --prefix apps/lex-admin run build",
    "start:admin": "npm --prefix apps/lex-admin run start",
    "build:back": "npm --prefix apps/lex-back run build",
    "start:back": "npm --prefix apps/lex-back run start"
  }
}
